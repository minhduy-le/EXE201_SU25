import { APP_COLOR } from "@/utils/constant";
import { View } from "react-native";

const Feedback = () => {
  return (
    <View
      style={{ backgroundColor: APP_COLOR.BACKGROUND_ORANGE, flex: 1 }}
    ></View>
  );
};
export default Feedback;
